本周我主要完成了HPL/HPCG基准测试的单进程运行任务，先是在WSL2环境下配置了编译依赖，尝试克隆HPL和HPCG的源码并进行编译，
按照要求生成了对应的运行配置文件，最终完成了单进程模式的基准测试，
也按任务要求整理了运行命令和输出结果，将文件提交到了指定的仓库目录中。 
在整个过程里，我最不熟悉的是HPCG的编译流程和参数配置部分，比如 make arch=Linux_MPI 指令的底层编译逻辑，
以及 hpcg.dat 文件中网格规模、迭代次数等参数的含义和设置依据，不清楚不同参数对测试结果的具体影响。 
遇到的问题主要是在WSL2中找不到Git仓库的正确路径，多次输入 cd 命令都提示目录不存在，
后来才发现仓库在Windows桌面，需要通过 /mnt/c 访问Windows磁盘；
另外在终端粘贴大段命令时还出现了格式错乱的情况，导致生成的Markdown文件内容混乱，最后改用Windows记事本手动创建文件才解决。
目前还困惑于HPL/HPCG多进程模式的运行方法，以及如何优化测试的性能指标，后续希望能进一步学习这些内容。
